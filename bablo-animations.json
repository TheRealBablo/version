{
    "version": "2.0.9",
    "descriptions": [
        "Added new client exports (isMenuOpen, disableCancelAnimation(state)).",
        "Introduced a new statebag (isInAnimation) that returns the currently playing animation or false if no animation is active.",
        "Removed unnecessary code from the UI.",
        "Previews are now in WEBP format instead of PNG, improving performance with around 80% reduction in file size."
    ]
}
