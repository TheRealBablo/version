{
    "version": "3.2.9",
    "descriptions": [
        "Added onCancelEmote flag — you can now define an emote that automatically plays when an active emote is canceled (e.g., onCancelEmote = \"emotename\").",
        "Added support for converted emotes to use both musicURL and onCancelEmote flags.",
        "Fixed an issue where musicURL wouldn't work correctly for shared emotes."
    ]
}
